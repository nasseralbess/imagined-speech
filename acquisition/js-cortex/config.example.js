// Copy this file to `config.js` and fill in your Emotiv Cortex credentials.
// `config.js` is git-ignored because it holds a secret.
// Get these from https://www.emotiv.com/developer/ (Cortex app registration).
export default {
    appId: 'your-app-id',
    license: 'your-license-key',
    clientId: 'your-client-id',
    clientSecret: 'your-client-secret',
    debit: 5000,
};
